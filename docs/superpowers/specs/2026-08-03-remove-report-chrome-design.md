# 去除报告四处冗余标识设计

**日期：** 2026-08-03
**状态：** 待用户复核

## 目标

从当前 PiGoo 经营诊断报告及未来生成的报告中去除以下四处展示元素：

1. 左侧目录栏顶部的“目录”标题；
2. 页面顶部的“小红书经营分析”；
3. 页面顶部的“Single-file HTML”；
4. 正文标题上方的“Integrated Report”或“Operating Report”胶囊标签。

目录导航链接、报告主标题、正文、图表、表格和单文件 HTML 能力保持不变。

## 实现范围

### 共享目录组件

调整 `xhs_ceramics_analytics/reporting/toc.py`，不再渲染 `toc-rail__title`，但继续渲染 `nav`、目录列表及锚点链接。同步更新相关样式和测试，避免留下无用标题节点。

### 两条 HTML 渲染路径

同时修改：

- `xhs_ceramics_analytics/reporting/html.py` 的 Markdown/叙事报告渲染路径；
- `xhs_ceramics_analytics/reporting/templates/report.html.j2` 的结构化事实报告渲染路径。

两条路径均删除整个顶部栏和标题上方胶囊标签，而不是用 CSS 隐藏。与这些节点仅相关、删除后不再使用的 CSS 一并清理。

### Skill 镜像

通过仓库现有同步机制，将根源码改动同步到 `skills/data-analyze-for-zcl/assets/xhs-ca/`，确保本仓库与安装/发布用 runtime 行为一致。

### 当前报告

使用当前运行目录中已经冻结的报告输入重新渲染截图对应的：

`.xhs-ceramics-analytics/outputs/20260803-021757-PiGoo手作瓷器2026年4-6月经营诊断报告/PiGoo手作瓷器2026年4-6月经营诊断报告.html`

只更新最终 HTML 展示，不重新计算事实、不改变叙事结论和数据。

## 兼容性与失败处理

- 目录仍须在宽屏显示为左侧栏，在窄屏显示为顶部导航；删除标题不改变响应式行为。
- HTML 仍须保持无外部资源依赖，并满足现有单文件报告契约。
- 若冻结输入无法安全重渲染，则停止覆盖当前文件并报告原因，不手工拼接业务内容。
- 保留工作区中与本任务无关的未提交改动，不回退、不覆盖。

## 验证

1. 根源码测试验证两条渲染路径均不再包含四项文本或对应冗余节点。
2. 目录测试验证锚点链接和目录条目仍然存在。
3. 单文件 HTML 契约测试验证无脚本、无网络依赖、标题唯一等约束保持通过。
4. 同步后在 skill 镜像中运行对应测试。
5. 检查当前 PiGoo HTML：四项文本为零，主标题唯一，目录链接仍可用。

## 非目标

- 不移除整个目录导航。
- 不修改报告标题、数据、结论、图表或表格。
- 不重做页面视觉风格，也不处理当前工作区中其他样式修改。
